include: package:flutter_lints/flutter.yaml

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options

analyzer:
  exclude:
    # Local build output; may hold throwaway evaluation scripts.
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
    # Separate Flutter projects with their own analysis options.
    - bench/**

linter:
  rules:
    public_member_api_docs: true
